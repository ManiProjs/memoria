export default function ErrorPage() {
    return (
      <div className="min-h-screen bg-zinc-950 text-white flex items-center justify-center">
        Failed to load Memoria. Make sure the server is actually running. If it's not, then sorry, we can't do it right now.
      </div>
    );
}